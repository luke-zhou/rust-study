/*
Remove Dups: Write code to remove duplicates from an unsorted linked list.
*/
use std::collections::{HashSet, LinkedList};
use std::hash::Hash;
use std::fmt::Display;

pub fn run<T>(list: &mut LinkedList<T>)
{
    let mut h: HashSet<&T> = HashSet::new();
    


}
